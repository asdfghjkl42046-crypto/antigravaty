## [開發原則 H] 黃金邏輯保護與雜草切除 (Golden Logic Protection)
- **核心精神**：在進行任何「清理（如 Lint 錯誤）」或「重構」前，必須先識別並宣告檔案中的「黃金邏輯（Golden Logic）」。
- **操作規範**：
    1.  **區分敵我**：AI 必須主動指出哪些是涉及核心演算法、物理補償、或加密鏈的關鍵代碼。
    2.  **建立共識**：在動手前，先向開發者說明該段邏輯的細節與「不可觸碰」的原因。
    3.  **精準切除**：僅對無關緊要的「雜草（Unused vars / any）」進行手術，確保黃金區塊的物理特徵（如順序、運算式）100% 不變。
- **目的**：防止 AI 為了追求代碼「乾淨」而誤殺具有深意的複雜邏輯，確保系統穩定性優先於規範。

## [開發原則 I] 零 Any 政策 (Anti-Any Policy)
- **核心禁令**：嚴禁在任何新開發或修復中使用 `any` 型別。這是導致系統難以維護與報錯連鎖反應的主因。
- **替代方案**：
    1.  **具體 Interface**：優先在 `types/game.ts` 中尋找或建立對應的 Interface。
    2.  **Unknown 斷言**：若數據來源極其複雜，使用 `unknown` 並搭配 `if` 型別守衛或 `as` 斷言。
    3.  **泛型 (Generics)**：在處理通用組件或函數時，使用泛型而非 `any`。
- **歷史債務處理**：在修復現有的 `any` 報錯時，必須先讀取數據結構，確保替換後的型別 100% 覆蓋原始數據需求，嚴禁「盲目轉型」。

---

## [2026-04-27] 事故 G：README 腦補與檔案管理失靈
- **現象**：AI 在 `CONSTITUTION.md` 中規定必須研讀 `README.md`，但實際檢查時發現根目錄並無此檔案。AI 隨後自行建立了一份全新的 `README.md`，導致歷史文檔內容可能遺失或產生斷層。
- **成因**：
    1.  **傲慢假設**：AI 憑直覺認為專案必有 README。
    2.  **查核不實**：在修改憲法時，未先以 `list_dir` 確認檔案存在，導致指令與現實脫鉤。
- **教訓**：在將任何檔案路徑寫入規則（Constitution/Rules）前，必須 100% 執行物理查核。嚴禁在規則中引用「不存在的幻覺檔案」。
- **修正**：已手動重建 `README.md` 並整合目前的法庭交互鏈邏輯。

## [2026-04-27] 事故 F：傲慢抄寫致物理引擎崩潰
- **現象**：新建立的起訴書（IndictmentBook）往回翻頁邏輯完全失效，出現角度穿透、封面消失、無法翻動等 Bug。
- **原因**：AI 助手在搬移「創業卷宗」黃金代碼時，犯下 **「傲慢之罪」**。自以為是地進行了所謂的「精簡」，私自刪除了 Ref-State 雙軌同步補償、GSAP 實時查詢與邊界防穿透判定。
- **後果**：嚴重破壞了手感的一致性，且因擅自刪除重要代碼造成開發進度倒退與開發者信任喪失。
- **戒律**：
    1. 當使用者提供已驗證的基準代碼時，AI 必須 **100% 逐行對齊**，嚴禁任何私自精簡或邏輯壓縮。
    2. 虛心接受人類調教出的物理細節，認清 AI 直覺與真實物理體感的差距。
    3. 執行前必須核對行數與邏輯分支完整性，不得先斬後奏。

---

## [2026-04-27] 事故 A：註解破碎導致全域編譯失敗
- **現象**：TypeScript 報錯「找不到名稱 'xxx'」，錯誤點指向中文字，導致全域無法編譯。
- **原因**：在合併 `SetupData` 至 `SystemStrings` 時，誤刪了檔案頂部的註解起始符 `/**`。
- **教訓**：搬移大規模文字時，必須檢查檔案頭尾格式。中文字若未被註解包裹會被視為變數，引發連鎖崩潰。

---

## [2026-04-27] 事故 B：iOS 輸入框點擊自動縮放
- **現象**：手機玩家點擊任何 input 時，畫面會突然放大並位移，導致 UI 跑版且無法手動縮回。
- **原因**：iOS Safari 規定字體小於 16px 的輸入框在聚焦時會自動觸發 Accessibility Zoom。
- **解決**：強制所有 input 為 `font-size: 16px` 並搭配 `user-scalable=no` 與 `touch-action: pan-x pan-y`。

---

## [2026-04-27] 事故 C：PaperFlip 分頁大字重複
- **現象**：卷宗翻頁組件（PaperFlip）的「首字放大（Drop Cap）」效果出現在每一頁的開頭。
- **原因**：`renderPage` 邏輯中未對頁碼 `idx` 進行判斷，僅檢查是否為段落開頭。
- **解決**：限制大字效果僅在 `idx === 1`（即封面後的第一頁內容）生效。

---

## [2026-04-27] 事故 D：Dashboard 標題拆分邏輯錯誤
- **現象**：首頁標題顯示為「安全冒險」而非「創業冒險」。
- **原因**：使用了 `.split('申報')[0]` 這種過於脆弱的字串拆分邏輯，當原始字串變動時導致結果非預期。
- **解決**：取消字串拆分邏輯，改為在 `SystemStrings.DECORATION.DASHBOARD_TITLE` 預定義 SSOT 標題。

---

## [2026-04-27] 事故 H：過度精簡致 ActionEngine 邏輯斷裂
- **現象**：修復 `any` 時導致 `ActionEngine.ts` 出現 10 處編編譯錯誤，關鍵變數 `snapshots` 消失，導致犯罪標籤無法上鏈。
- **成因**：
    1. **變數誤殺**：AI 在進行型別重構時，自作聰明地將 `snapshots` 改名為 `appliedTags`，卻未同步檢查下游 600 行代碼的引用。
    2. **忽視副作用**：移除 `as any` 後未考慮到屬性存在性（Optional Properties），導致 TS 報錯。
- **教訓**：
    - **原則 J：變數一致性守恆 (Variable Consistency)**
      - 修改大型函數（> 500行）時，嚴禁更換核心變數名稱。
    - **原則 K：型別補強優先於強轉 (Type Expansion > Casting)**
      - 優先修改 `types/game.ts` 賦予物件索引能力（如 `Card` 加入數字索引簽章），而非使用 `as any`。

---

## [2026-04-27] 事故 I：工具幻覺致回傳結構變異 (重大事故)
- **現象**：在使用 `multi_replace_file_content` 修復 `ActionEngine.ts` 時，AI 幻覺產生了不存在的 `actionResult` 變數宣告，並錯誤地覆寫了 `return` 語句。
- **成因**：
    1. **過度預測**：AI 工具在嘗試合併修改時，自行「腦補」了簡化後的代碼結構，卻未對齊檔案中數百行後的原始結構。
    2. **語境丟失**：大檔案（>600行）在進行區塊取代時，容易遺失變數的原始型別定義（如 `apRefunded` 被改為 `!!apRefunded`）。
- **教訓**：
    - **原則 L：核心引擎嚴禁大規模自動取代 (Manual Review for Core)**
      - 針對 `ActionEngine`, `CourtEngine` 等核心檔案，若修改範圍涉及 `return` 或 `interface` 實現，必須先以 `view_file` 讀取完整上下文，並優先採用 `replace_file_content` (單點取代) 而非 `multi_replace`。
    - **原則 M：零容忍「幻覺代碼」**
      - 任何由工具產生的 `any` 或是未定義變數（如 `actionResult`），必須在 5 分鐘內手動回歸原點，嚴禁將其留在專案中。

---
*文件更新：2026-04-27*
